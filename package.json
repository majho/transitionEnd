{
  "name": "transition-end",
  "version": "0.1.0",
  "description": "Tiny transitionEnd event helper",
  "module": "src/index.js",
  "scripts": {
    "lint": "eslint src",
    "clean": "rm -rf dist",
    "bundle": "rollup -c",
    "minify": "uglifyjs dist/transition-end.js -o dist/transition-end.js --compress --mangle",
    "build": "npm run lint && npm run clean && npm run bundle && npm run minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "transition",
    "transition event",
    "transitionEnd"
  ],
  "author": {
    "name": "Martin Johansson",
    "url": "http://majho.se/"
  },
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "eslint": "^4.14.0",
    "eslint-config-airbnb-base": "^11.3.2",
    "eslint-plugin-import": "^2.8.0",
    "rollup": "^0.47.6",
    "rollup-plugin-babel": "^3.0.2",
    "uglify-js": "^3.0.27"
  }
}
